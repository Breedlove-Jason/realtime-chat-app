{
 "name": "relay-chat", "private": true, "version": "1.0.0", "engines": {"node":"24.x"},
 "scripts": { "build": "npm ci --prefix backend && npm ci --include=dev --prefix frontend && npm run build --prefix frontend", "start": "npm start --prefix backend", "test": "npm test --prefix backend", "lint": "npm run lint --prefix frontend" }
}
